// {{ ansible_managed }}

{% if p_zones is defined %}
{% for zone in p_zones.files %}
zone "{{ zone.path|replace("/etc/named/primaries/db.", "") }}" {
        type master;
        file "{{ zone.path }}";
};
{% endfor %}
{% endif %}

